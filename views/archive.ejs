<!DOCTYPE html>
<html lang="en" class="h-full">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/styles/main.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/prism/1.30.0/themes/prism.min.css">
</head>
<body class="h-full bg-gray-50 flex flex-col">
    <div class="dot-grid-background"></div>
    <header class="bg-white shadow-sm">
        <div class="blog-container">
            <div class="blog-nav">
                <h1 class="text-2xl font-bold text-gray-900">
                    <a href="/" class="hover:text-blue-600 transition-colors">My Blog</a>
                </h1>
                <nav class="blog-nav-links">
                    <a href="/" class="blog-nav-link">Home</a>
                    <a href="/posts" class="blog-nav-link">Posts</a>
                    <a href="/about" class="blog-nav-link">About</a>
                </nav>
            </div>
        </div>
    </header>
    
    <main class="flex-1">
        <div class="blog-container">
            <div class="blog-header">
                <div class="mb-4">
                    <a href="/" class="text-blue-600 hover:text-blue-800 text-sm font-medium">
                        ← Back to Home
                    </a>
                </div>
                <h2 class="blog-title"><%= year %> Archive</h2>
                <p class="blog-subtitle"><%= posts.length %> post<%= posts.length !== 1 ? 's' : '' %> from <%= year %></p>
            </div>
            
            <% if (posts && posts.length > 0) { %>
                <div class="space-y-8">
                    <% 
                    // Group posts by month
                    const postsByMonth = {};
                    posts.forEach(post => {
                        const month = new Date(post.meta.date).toLocaleDateString('en-US', { month: 'long' });
                        if (!postsByMonth[month]) postsByMonth[month] = [];
                        postsByMonth[month].push(post);
                    });
                    
                    Object.keys(postsByMonth).forEach(month => {
                    %>
                        <div class="mb-12">
                            <h3 class="text-xl font-semibold text-gray-900 mb-6 pb-2 border-b border-gray-200"><%= month %> <%= year %></h3>
                            <div class="space-y-6">
                                <% postsByMonth[month].forEach(post => { %>
                                    <article class="blog-card">
                                        <h4 class="blog-card-title">
                                            <a href="/posts/<%= post.meta.slug %>" class="hover:text-blue-600 transition-colors">
                                                <%= post.meta.title %>
                                            </a>
                                        </h4>
                                        <p class="blog-card-excerpt"><%= post.excerpt %></p>
                                        <div class="blog-card-meta">
                                            <span><%= new Date(post.meta.date).toLocaleDateString('en-US', { month: 'long', day: 'numeric' }) %></span>
                                            <span>·</span>
                                            <span><%= post.readingTime %> min read</span>
                                            <% if (post.meta.tags && post.meta.tags.length > 0) { %>
                                                <span>·</span>
                                                <% post.meta.tags.slice(0, 2).forEach((tag, index) => { %>
                                                    <a href="/labeled/<%= tag.toLowerCase() %>" class="text-blue-600 hover:text-blue-800 text-sm">
                                                        <%= tag %><%= index < Math.min(post.meta.tags.length, 2) - 1 ? ',' : '' %>
                                                    </a>
                                                <% }); %>
                                            <% } %>
                                        </div>
                                    </article>
                                <% }); %>
                            </div>
                        </div>
                    <% }); %>
                </div>
            <% } else { %>
                <div class="text-center py-12">
                    <h3 class="text-lg font-medium text-gray-900 mb-2">No posts found</h3>
                    <p class="text-gray-600">No posts were published in <%= year %>.</p>
                    <a href="/" class="inline-block mt-4 text-blue-600 hover:text-blue-800">← Back to Home</a>
                </div>
            <% } %>
        </div>

        <% if (archiveData && Object.keys(archiveData).length > 1) { %>
        <div class="bg-white border-t border-gray-200 mt-12">
            <div class="blog-container py-8">
                <h3 class="text-lg font-semibold text-gray-900 mb-4">Other Years</h3>
                <div class="flex flex-wrap gap-2">
                    <% Object.keys(archiveData).filter(y => y !== year).sort().reverse().forEach(y => { %>
                        <a href="/archive/<%= y %>" class="inline-flex items-center px-3 py-1 rounded-full text-sm font-medium bg-gray-100 text-gray-800 hover:bg-gray-200 transition-colors">
                            <%= y %> 
                            <span class="ml-1 text-gray-600">(<%= archiveData[y] %>)</span>
                        </a>
                    <% }); %>
                </div>
            </div>
        </div>
        <% } %>
    </main>
    
    <footer class="bg-white border-t border-gray-200 mt-12">
        <div class="blog-container">
            <div class="blog-footer">
                <div class="flex flex-col md:flex-row justify-between items-center">
                    <p class="text-gray-600 mb-4 md:mb-0">&copy; 2025 My Blog. Built with Koa.js and Tailwind CSS.</p>
                    <div class="flex space-x-4">
                        <a href="/feed.xml" class="text-gray-500 hover:text-gray-700 transition-colors">RSS</a>
                        <a href="/sitemap.xml" class="text-gray-500 hover:text-gray-700 transition-colors">Sitemap</a>
                    </div>
                </div>
            </div>
        </div>
    </footer>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/prism/1.30.0/components/prism-core.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/prism/1.30.0/plugins/autoloader/prism-autoloader.min.js"></script>
</body>
</html>
