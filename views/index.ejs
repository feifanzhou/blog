<!DOCTYPE html>
<html lang="en" class="h-full">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/styles/main.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/prism/1.30.0/themes/prism.min.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:ital,wght@0,400;0,700;1,400;1,700&display=swap" rel="stylesheet">
</head>
<body class="h-full bg-white flex flex-col font-mono">
    <main class="flex-1 max-w-5xl mx-auto px-8 py-12">
        <article class="magazine-layout">
            <header class="magazine-header">
                <div class="magazine-masthead">
                    <h1 class="magazine-title">
                        BLOG INDEX
                    </h1>
                    <div class="magazine-date">
                        <%= new Date().toISOString().split('T')[0] %>
                    </div>
                </div>
                <div class="magazine-border"></div>
            </header>

            <div class="magazine-content">
                <div class="magazine-sidebar">
                    <div class="magazine-section-label">
                        ALL<br>
                        ARTICLES<br>
                        IN<br>
                        REVERSE<br>
                        CHRONOLOGY
                    </div>

                    <div class="magazine-section-number">
                        Index<br>
                        <%= new Date().getMonth() + 1 %>.<%= String(new Date().getDate()).padStart(2, '0') %>
                    </div>
                </div>

                <div class="magazine-text">
                    <div class="magazine-content-body">
                        <% if (posts && posts.length > 0) { %>
                            <div class="magazine-index">
                                <% posts.forEach((post, index) => { %>
                                    <div class="magazine-index-item">
                                        <div class="magazine-index-number"><%= String(index + 1).padStart(2, '0') %>.</div>
                                        <div class="magazine-index-content">
                                            <h3 class="magazine-index-title">
                                                <a href="/posts/<%= post.meta.slug %>"><%= post.meta.title.toUpperCase() %></a>
                                            </h3>
                                            <div class="magazine-index-meta">
                                                <span class="magazine-index-date"><%= new Date(post.meta.date).toISOString().split('T')[0] %></span>
                                                <% if (post.meta.tags && post.meta.tags.length > 0) { %>
                                                    <span class="magazine-index-separator">—</span>
                                                    <span class="magazine-index-tag"><%= post.meta.tags[0].toUpperCase() %></span>
                                                <% } %>
                                            </div>
                                            <% if (post.meta.excerpt || post.excerpt) { %>
                                                <p class="magazine-index-excerpt"><%= post.meta.excerpt || post.excerpt %></p>
                                            <% } %>
                                        </div>
                                    </div>
                                <% }); %>
                            </div>
                        <% } else { %>
                            <div class="magazine-no-posts">
                                <p>NO ARTICLES AVAILABLE AT THIS TIME.</p>
                                <p>CHECK BACK SOON FOR NEW TECHNICAL DOCUMENTATION.</p>
                            </div>
                        <% } %>
                    </div>

                    <div class="magazine-page-number">
                        INDEX PAGE 01
                    </div>
                </div>
            </div>
        </article>
    </main>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/prism/1.30.0/components/prism-core.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/prism/1.30.0/plugins/autoloader/prism-autoloader.min.js"></script>
</body>
</html>
